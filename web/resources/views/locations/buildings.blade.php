@extends('layouts.app') @section('title','Buildings') @section('content')
<div class="flex gap-4"><a href="{{route('campuses.index')}}">Campuses</a><a href="{{route('locations.index')}}">Offices / Rooms / Areas</a></div><h1 class="mt-3 text-2xl font-semibold">Buildings</h1><form class="mt-3 flex gap-2" method="GET"><select name="campus_id" class="rounded border p-2"><option value="">All campuses</option>@foreach($campuses as $campus)<option value="{{$campus->id}}">{{$campus->name}}</option>@endforeach</select><input name="search" class="rounded border p-2" placeholder="Name or code"><button class="rounded bg-slate-700 px-3 text-white">Filter</button></form><div class="mt-5 grid gap-5 md:grid-cols-2"><div class="rounded border bg-white p-5"><table class="w-full text-sm">@forelse($buildings as $building)<tr><td><a class="text-blue-700 underline" href="{{route('buildings.show',$building)}}">{{$building->name}}</a><br><small>{{$building->campus->name}}</small></td><td>{{$building->is_active?'Active':'Inactive'}}</td></tr>@empty<tr><td>No buildings have been added.</td></tr>@endforelse</table></div>@can('buildings.create')<form method="POST" action="{{route('buildings.store')}}" class="rounded border bg-white p-5">@csrf<h2 class="font-semibold">Add building</h2><select class="mt-3 w-full rounded border p-2" name="campus_id" required>@foreach($campuses as $campus)<option value="{{$campus->id}}">{{$campus->name}}</option>@endforeach</select><input class="mt-3 w-full rounded border p-2" name="name" placeholder="Building name" required><input class="mt-3 w-full rounded border p-2" name="code" placeholder="Code"><input type="hidden" name="is_active" value="1"><button class="mt-4 rounded bg-blue-700 px-4 py-2 text-white">Save building</button></form>@endcan</div>@endsection
